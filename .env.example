# Supabase (БД + авторизация) — нужны для входа
# URL проекта и ПУБЛИЧНЫЙ anon/publishable ключ (НЕ secret!)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Groq (AI-оценщик сложности, бесплатный tier) — нужен для /api/evaluate
GROQ_API_KEY=
# Claude API (AI-оценщик) — добавим позже
ANTHROPIC_API_KEY=
