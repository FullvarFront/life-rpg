# src/components — UI-компоненты

Переиспользуемые React-компоненты. Только отображение.

- Не содержат игровой логики (она в `src/game/`).
- Не делают сетевых запросов и не обращаются к БД напрямую.
- Получают данные через props.
